# GitHub 쓰기 진단 / 재개 절차

검토일: 2026-09-24. 사용자 요청: 쓰기가 간헐적으로 불가능하다고 안내한 원인을 구분하고 실제 쓰기 경로를 검증한다. 이 문서는 진단 기록이며 권한 확대나 안전장치 우회 승인이 아니다.

## CHECKPOINT-028 / 이번 확인

- 기존 작업 계정 illumin8-dev와 작업 저장소 illumin8-dev/richon-academy를 사용했다. 다른 연결 계정으로 바꾸지 않았다.
- 현재 도구 검색에서 update_file이 노출됐고 create_file / update_ref / update_pull_request 등 쓰기 동작의 명시적 입력 계약도 제공됐다.
- get_repo의 저장소 메타데이터는 pull:true / push:true를 반환했다. 이 값만으로 모든 GitHub App API 권한이 충족됐다고 판단하지 않는다.
- GitHub 앱별 승인 설정은 Allow all actions, 전역 기본값은 Allow low-risk actions로 조회됐다. 이번에 어느 설정도 바꾸지 않았다.
- main 루트 목록에 이 문서가 없는 것을 확인하고 create_file로 이 진단 기록을 작성한다. 쓰기 성공 여부는 반환된 commit과 해당 commit에서의 파일 재조회로 확인한다. 문서 본문에 쓰기 응답이 오기 전의 성공을 선기재하지 않는다.
- 운영 Cloud Run, Worker, DB, IAM, OAuth 키, 접근 제한과 배포 요청 파일은 이번 진단에서 변경하지 않는다.

## 관측과 추정을 구분한 원인 분석

### 1. 직전 문제: 도구 노출과 GitHub 권한을 혼동

대화에 보존된 직전 도구 검색 결과에서는 create_tree / update가 빈 결과였고, 전체 검색은 48개 동작을 반환했다. 당시 실제 파일 수정 요청이 GitHub에서 403으로 실패한 기록은 없다. 이번에는 기존 연결로 update_file 검색이 동작 스키마를 제공했다.

확인 가능한 결론은 '관측된 호출 도구 목록이 시점에 따라 달랐다'이다. 계정 쓰기 권한이 없어졌다거나 사용자가 권한을 취소했다고 볼 근거는 없다. 도구 목록 차이가 연결 로딩, 카탈로그 갱신, 실행 표면/모델 변경 중 무엇 때문인지는 내부 진단자료가 없어 확정하지 못했다. 재조회가 차이를 일으킨 원인이라고도 단정하지 않는다.

이전 안내에서 현재 도구 목록의 한계를 프로젝트 전체의 쓰기 불가로 확대하고 사용자에게 작업을 넘긴 점을 바로잡는다. 그렇다고 앞으로 도구가 반드시 항상 노출된다고 보장하지 않는다.

### 2. 별도 문제: 원본 저장소의 API 403

과거 marururu00/richon-academy 원본에 대한 쓰기 요청의 Resource not accessible by integration은 API 권한/설치 범위 문제로 분류한다. 작업 fork illumin8-dev의 쓰기 가능 여부와 별개다. 공개 파일을 읽을 수 있어도 동일한 연결로 쓸 수 있다는 뜻은 아니다.

이번 진단은 작업 저장소에 한정한다. 원본에 무관한 시험 쓰기를 만들지 않았으며 원본의 쓰기 권한까지 복구됐다고 표시하지 않는다. 사용자 직접 원본 PR8을 병합하고 히어로를 확인한 이력은 보존한다.

### 3. 또 다른 문제: Cloudflare BIC 403

이전 배포 검사 요청이 BIC에 차단된 것은 웹 요청 단계다. GitHub Contents API 쓰기 권한 문제가 아니다. 기존 CHECKPOINT027은 소유자가 예외를 설정한 후 접근 확인과 카카오/네이버 후보 기동이 성공했다고 기록한다. 이 진단 때문에 DB008, 네이버 Secret 권한, BIC 설정을 재실행하지 않는다.

### 4. 승인 설정과 외부 권한은 서로 다름

앱의 Allow all actions는 지원되는 동작을 사용할 때의 승인 정책이다. 없는 동작을 만들거나 외부 서비스의 권한을 부여하지 않는다. 현재 쓰기 문제 해결을 위해 전역 권한, GitHub 설치 범위, 브랜치 보호, 토큰 또는 Cloudflare 보안을 넓히지 않는다.

## 다음 작업에서 쓰기 경로를 확인하는 순서

1. 사용자가 승인한 대상 저장소, 연결 계정, 브랜치, 작업 범위를 먼저 고정한다. 이 프로젝트 작업 계정은 illumin8-dev다. 개인 계정이나 다른 저장소로 임의 전환하지 않는다.
2. 필요한 동작 스키마가 없으면 GitHub 도구에서 정확한 동작명(update_file, create_file, create_pull_request 등)으로 조회한다. 후보 이름/단일 검색의 빈 결과만으로 지원하지 않는다고 단정하지 않는다. 필요한 경우 필터 없는 목록을 한 번 확인한다. 무한 재검색하지 않는다.
3. 스키마가 있으면 읽기 조회 후 그 동작을 직접 사용한다. 일반 fetch는 GET 전용이므로 파일 쓰기 수단으로 쓰지 않는다. create_file에 텍스트, update_file에는 전체 UTF-8 본문과 최신 파일 blob SHA를 전달한다. commit SHA와 blob SHA를 혼동하지 않는다.
4. 처음부터 대용량 코드나 배포 설정을 시험 쓰기로 사용하지 않는다. 실제 필요한 비민감 진행 기록 같은 작은 승인 범위 변경으로 확인한다. 댓글 쓰기 성공만으로 파일 쓰기가 검증됐다고 하지 않는다.
5. 성공 응답의 commit SHA와 파일 내용을 다시 읽어 일치 여부를 확인한다. 같은 경로를 병렬 수정하지 않는다. 응답이 불명확하면 먼저 원격 내용을 확인해 중복 커밋/중복 PR을 방지한다.
6. 동작이 정말 미노출이면 'GitHub 권한 없음' 대신 '이 실행에서 필요한 동작 스키마를 확인하지 못함'으로 보고한다. 현재 정식으로 제공되고 인증된 CLI 쓰기 경로가 있는 경우에만 그 계약과 동일한 권한/승인 범위 안에서 사용한다. 정식 API가 명시적으로 거부한 권한을 우회하지 않는다.
7. 복구가 안 되면 승인된 수정본, 기준 SHA, 검증 증거, 마지막 성공한 원격 작업을 보존한다. 자동화/원격 작업을 시작하지 않고 시작했다고 말하지 않는다. 다음 대화에서는 과거 실패 결론 대신 새로 제공된 도구와 원격 상태부터 확인한다.

## 실패 유형별 처리

| 관측 | 분류 | 다음 확인 |
|---|---|---|
| 도구 검색의 빈 결과 / 필요한 스키마 없음 | 동작 발견·노출 | 정확한 이름과 전체 목록을 제한적으로 확인; API 권한 실패와 구분 |
| 401 또는 재연결 요구 | 인증 | 해당 연결만 확인; 키를 대화에 붙이게 하지 않음 |
| GitHub 403 Resource not accessible by integration | 외부 API 권한 | 실제 대상 저장소와 API 권한/설치 범위 확인; 다른 쓰기 경로로 회피하지 않음 |
| 404 | 대상·ref 또는 접근 범위 | 정확한 저장소/브랜치/경로 확인; 곧바로 파일 삭제로 단정하지 않음 |
| 409 또는 SHA 불일치 | 동시 수정·오래된 버전 | 최신 파일/commit 재조회 및 변경 조정; force 금지 |
| 422 | 입력·리소스 상태 | 도구 계약과 GitHub 오류를 대조; 동일 요청 반복 금지 |
| 타임아웃 / 불명확한 결과 | 성공 여부 불명확 | 원격 파일/PR 상태를 먼저 읽은 뒤 제한적으로 재시도 |
| 명시적 안전·승인 거절 | 승인/안전 경계 | 중단하고 설명/정식 승인; 요청 분할·인코딩·다른 도구로 우회 금지 |
| 웹사이트 403 | 별도 런타임 네트워크 경계 | 웹 이벤트와 배포 로그 확인; GitHub 권한을 변경하지 않음 |

## 이어갈 프로젝트 작업

마지막 확인된 운영 근거는 CHECKPOINT027이다. 후보 richon-portal-gh-35946050229-1와 Naver 두 Secret 버전1의 준비가 확인됐고, 기존 주소의 전환은 아직이다. 이번 쓰기 진단은 클라우드 상태를 독립 재조회한 것은 아니다.

사용자는 이미 본인 이메일 Access 제한을 유지한 후보 연결을 승인했다. 이전 대화에서 준비한 richon-login-transition-verified.zip은 로컬 보관본이며 원격 반영 증거가 아니었다. 재개 시 정확한 파일 존재와 기준 SHA, diff, 테스트를 확인한다. Worker 변경 전 후보/tag와 기존 롤백 대상을 확인하고, 실제 서버키를 읽거나 사용자에게 복사시키지 않는다. main을 Worker 운영 브랜치로 덮어쓰지 않는다. 개인정보·탈퇴와 PR10은 계속 후속 범위다.

## 참고 근거

이번 원인 판정은 도구 호출 결과와 대화에 보존된 오류에 근거한다. 아래 공식 문서는 권한 층위와 일반적인 진단 방법을 설명하며, 이번 도구 목록 변경의 내부 원인을 설명하는 자료는 아니다.

- https://help.openai.com/en/articles/20001497-troubleshooting-apps-in-chatgpt
- https://help.openai.com/en/articles/20001495-managing-app-permissions-in-chatgpt
- https://docs.github.com/en/rest/using-the-rest-api/troubleshooting-the-rest-api
- https://docs.github.com/en/rest/repos/contents#create-or-update-file-contents
